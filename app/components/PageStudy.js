import React from 'react';

import StudyCard from './StudyCard';

const PageStudy = React.createClass({
  render () {
    return (
      <StudyCard showBack={false} />
    );
  }
});

export default PageStudy;
